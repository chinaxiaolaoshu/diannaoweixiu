const postgres=require("postgres");const u=new URL(process.env.DATABASE_URL);const sql=postgres({host:u.hostname,port:Number(u.port),username:u.username,password:decodeURIComponent(u.password),database:u.pathname.slice(1),ssl:{servername:u.hostname,rejectUnauthorized:false},prepare:false,connect_timeout:15});(async()=>{try{const r=await sql`SELECT count(*)::int AS n FROM articles`;console.log("OK articles=",r[0].n)}catch(e){console.log("FAIL:",e.code,e.message.slice(0,150))}finally{await sql.end({timeout:5})}})()
